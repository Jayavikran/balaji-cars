# Leave unset to use the Vite dev proxy (see vite.config.ts) which forwards
# /api requests to http://localhost:5000 during local development.
# Set this to your deployed backend URL in production.
VITE_API_URL=https://balaji-cars.onrender.com/api